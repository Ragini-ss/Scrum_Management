from django.conf.urls import url

from . import views

urlpatterns = [
    # url(r'^register/$',views.MemberRegistrationView.as_view(), name = 'member_registration'),
    # url(r'^myprojects/$', views.MemberAssignProjectView.as_view(), name = 'member_project_list'),
]
